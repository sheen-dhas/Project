---
platform: linux

image_resource:
  type: docker-image
  source: 
         repository: nerdammer/hbase-phoenix
         

inputs:
- name: resource-tutorial

run:
  path: sh
  args:
        - -exc
        - |
          cd resource-tutorial/proj2
          chmod 777 run_spark.yml
          ./run_spark.yml
