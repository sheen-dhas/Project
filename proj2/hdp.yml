platform: linux

image_resource:
  type: docker-image
  source: 
         repository: nerdammer/hbase-phoenix

inputs:
- name: resource-tutorial
- name: build-spark
- name: build-hbase


run:
  path: sh
  args:
        - -exc
        - |
          ls
          ##cp -r build-spark /home
          ln -s build-spark/rootfs/usr/spark-2.2.0/bin/spark-submit home
          ##chmod 777 spark-submit
          cd resource-tutorial/proj2
          chmod 777 run_h.sh
          ./run_h.sh
