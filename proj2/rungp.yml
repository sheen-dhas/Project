---
platform: linux

image_resource:
  type: docker-image
  source: 
         repository: pivotaldata/gpdb-base
         username:root
         password:pivotal

inputs:
- name: resource-tutorial

run:
    path: echo
    args: ["Hello, world!"]
