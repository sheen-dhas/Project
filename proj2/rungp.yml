---
platform: linux

image_resource:
  type: docker-image
  source: 
         repository: pivotaldata/gpdb-base
         port : 2022
         username: gpadmin
         password: pivotal

inputs:
- name: resource-tutorial

run:
    path: echo
    args: ["Hello, world!"]
